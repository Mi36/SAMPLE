import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  safeAreaView: {
    flex: 1,
  },
  header: {
    alignSelf: 'center',
    fontWeight: 'bold',
    fontSize: 35,
  },
});

export default styles;
